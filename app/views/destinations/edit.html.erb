<div class="header">
	<div class="container">
		<img src="/assets/ruby.png" width="80">
		<h1>TravelApp</h1>
	</div>
</div>


<%= form_for(@destination) do |f| %>
	<%= f.text_area :name %>
	<%= f.text_area :description %>
	<%= f.submit "Make changes" %>
<% end %>

<%= link_to "Back", @tag %>